export { CameraManager } from "./CameraManager";
export {
  CAMERA_OFFSET,
  DEFAULT_CAMERA_DISTANCE,
  DEFAULT_CAMERA_POSITION,
  DEFAULT_CAMERA_TARGET,
  DEFAULT_FOV,
} from "./constants";

// Re-export types from @teskooano/data-types for convenience
export type {
  ICameraRenderer,
  CameraManagerState,
  CameraManagerOptions,
} from "@teskooano/data-types";
