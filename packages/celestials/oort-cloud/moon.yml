$schema: "https://moonrepo.dev/schemas/project.json"
language: "typescript"
platform: "node"
type: "library"

id: "oort-cloud"

tasks:
  lint:
    command: "eslint ."
  test:
    command: "vitest run"
