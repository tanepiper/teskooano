$schema: "https://moonrepo.dev/schemas/project.json"
language: "typescript"
platform: "node"
type: "library"

id: "celestials-oort-cloud"

tasks:
  lint:
    command: "eslint ."
  test:
    command: "vitest run"
