$schema: "https://moonrepo.dev/schemas/project.json"
language: "typescript"
platform: "node"
type: "library"

id: "celestials-stars"

tasks:
  lint:
    command: "eslint ."
  test:
    command: "vitest run"
