type: library
language: typescript

id: "core/physics"

tasks:
  test:
    command: npm run test
    local: true
    options:
      interactive: true
    inputs:
      - src/**/*.ts
      - src/**/*.tsx
