type: library
language: typescript

id: core-state

tasks:
  test:
    command: npm run test
    local: true
    options:
      interactive: true
    inputs:
      - src/**/*.ts
      - src/**/*.tsx
      - vitest.config.ts
